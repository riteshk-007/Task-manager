import LoginPage from "@/components/LoginPage";

export const metadata = {
  title: "Login Page",
};
const page = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default page;
